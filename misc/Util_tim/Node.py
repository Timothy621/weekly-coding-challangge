
class Node:
    # creates the node with the variables data and next
    # data which stores the information we want
    # and Next that will store our next node.
    def __init__(self, data):
        self.data = data
        self.next = None

    # def __repr__(self):
    #     return str(self)
